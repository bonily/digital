import styled from 'styled-components';

export const MAX_WIDTH_DESKTOP = `1366px`;

export const MAIN_TEXT_FONT = `Gilroy`;

export const TITLE_FONT = `Galien`;

export const H2 = styled.h2`
display: inline-block;
max-width: 230px;


margin: 0;
margin-right: 89px;


font-family: ${MAIN_TEXT_FONT};
font-size: 36px;
line-height: 46px;

letter-spacing: 0.05em;

color: #FFFFFF;
`;

export const P = styled.p`
  font-family: ${MAIN_TEXT_FONT};
  font-size: 16px;
  line-height: 26px;

  letter-spacing: 0.05em;

  color: #FFFFFF;
  opacity: 0.6;
`;
